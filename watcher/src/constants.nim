## Build-time configuration for the watcher. Fill in your own values before
## compiling, and never commit real tokens or keys to a public repository.

const
  WatcherToken* = "YOUR_WATCHER_BOT_TOKEN"
  WatcherBotId* = "YOUR_WATCHER_BOT_USER_ID"
  TargetGuildId* = "YOUR_GUILD_ID"
  DropzoneChannelId* = "YOUR_DROPZONE_CHANNEL_ID"
  ActiveCategoryId* = "YOUR_ONLINE_CATEGORY_ID"
  OfflineCategoryId* = "YOUR_OFFLINE_CATEGORY_ID"
  SharedKey* = "YOUR_SHARED_PAYLOAD_KEY"
