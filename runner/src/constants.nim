## Build-time configuration for the runner. Fill in your own values before
## compiling, and never commit real tokens or keys to a public repository.

const
  RunnerToken* = "YOUR_RUNNER_BOT_TOKEN"
  TargetGuildId* = "YOUR_GUILD_ID"
  DropzoneChannelId* = "YOUR_DROPZONE_CHANNEL_ID"
  SharedKey* = "YOUR_SHARED_PAYLOAD_KEY"
